<Window x:Class="IcdControl.Client.ModernDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dialog" SizeToContent="Height" Width="460" MinHeight="200" MinWidth="360"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource BackgroundColor}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="{DynamicResource SurfaceColor}" CornerRadius="10" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Padding="18">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="TitleTxt" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryColor}" TextWrapping="Wrap"/>
                <TextBlock x:Name="MessageTxt" Grid.Row="1" Margin="0,10,0,0" FontSize="13" Foreground="{DynamicResource TextSecondaryColor}" TextWrapping="Wrap"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="SecondaryBtn" Style="{StaticResource SecondaryBtn}" MinWidth="90" Margin="0,0,10,0" Click="Secondary_Click"/>
            <Button x:Name="PrimaryBtn" Style="{StaticResource ModernBtn}" MinWidth="90" Click="Primary_Click"/>
        </StackPanel>
    </Grid>
</Window>
