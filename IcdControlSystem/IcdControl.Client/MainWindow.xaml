<Window x:Class="IcdControl.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IcdControl.Client"
        mc:Ignorable="d"
        Title="ICD Manager" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,8">
            <Button x:Name="RefreshBtn" Content="Refresh" Click="RefreshBtn_Click" Width="90" Margin="0,0,8,0" />
            <Button x:Name="NewBtn" Content="New ICD" Click="NewBtn_Click" Width="90" Margin="0,0,8,0" />
            <Button x:Name="OpenBtn" Content="Open" Click="OpenBtn_Click" Width="90" Margin="0,0,8,0" />
            <Button x:Name="ExportBtn" Content="Export .h" Click="ExportBtn_Click" Width="90" />
        </StackPanel>

        <DataGrid x:Name="IcdGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="120" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200" />
                <DataGridTextColumn Header="Messages" Binding="{Binding Messages.Count}" Width="90" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
