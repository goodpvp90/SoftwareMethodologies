<Window x:Class="IcdControl.Client.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="500" Width="400"
    WindowStartupLocation="CenterOwner" Background="{DynamicResource BackgroundColor}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Settings" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Appearance" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryColor}" Margin="0,0,0,15"/>
            
            <CheckBox x:Name="DarkModeChk" Content="Dark Mode" FontSize="14" Foreground="{DynamicResource TextPrimaryColor}" Margin="0,0,0,15" Checked="DarkModeChk_Checked" Unchecked="DarkModeChk_Unchecked"/>

            <TextBlock Text="Keyboard Shortcuts" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryColor}" Margin="0,20,0,15"/>
            <TextBlock Text="Ctrl+N - New ICD" FontSize="12" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,0,0,5"/>
            <TextBlock Text="Ctrl+O - Open Editor" FontSize="12" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,0,0,5"/>
            <TextBlock Text="Ctrl+S - Save" FontSize="12" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,0,0,5"/>
            <TextBlock Text="Ctrl+F - Search" FontSize="12" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,0,0,5"/>
            <TextBlock Text="Ctrl+Z - Undo" FontSize="12" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,0,0,5"/>
            <TextBlock Text="Ctrl+Y - Redo" FontSize="12" Foreground="{DynamicResource TextSecondaryColor}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save" Style="{StaticResource ModernBtn}" Click="SaveBtn_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Cancel" Style="{StaticResource SecondaryBtn}" Click="CancelBtn_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>

