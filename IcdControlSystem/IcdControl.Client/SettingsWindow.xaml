<Window x:Class="IcdControl.Client.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="500" Width="400"
        WindowStartupLocation="CenterOwner" Background="{StaticResource BackgroundColor}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Settings" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Appearance" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
            
            <CheckBox x:Name="DarkModeChk" Content="Dark Mode" FontSize="14" Margin="0,0,0,15" Checked="DarkModeChk_Checked" Unchecked="DarkModeChk_Unchecked"/>

            <TextBlock Text="Language" FontSize="14" Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,5"/>
            <ComboBox x:Name="LanguageCombo" Height="35" Margin="0,0,0,20">
                <ComboBoxItem Content="English" Tag="en" IsSelected="True"/>
                <ComboBoxItem Content="עברית" Tag="he"/>
            </ComboBox>

            <TextBlock Text="Theme" FontSize="14" Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,5"/>
            <ComboBox x:Name="ThemeCombo" Height="35" Margin="0,0,0,20">
                <ComboBoxItem Content="Default" Tag="default" IsSelected="True"/>
                <ComboBoxItem Content="Blue" Tag="blue"/>
                <ComboBoxItem Content="Green" Tag="green"/>
            </ComboBox>

            <TextBlock Text="Keyboard Shortcuts" FontSize="18" FontWeight="SemiBold" Margin="0,20,0,15"/>
            <TextBlock Text="Ctrl+N - New ICD" FontSize="12" Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,5"/>
            <TextBlock Text="Ctrl+O - Open Editor" FontSize="12" Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,5"/>
            <TextBlock Text="Ctrl+S - Save" FontSize="12" Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,5"/>
            <TextBlock Text="Ctrl+F - Search" FontSize="12" Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,5"/>
            <TextBlock Text="Ctrl+Z - Undo" FontSize="12" Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,5"/>
            <TextBlock Text="Ctrl+Y - Redo" FontSize="12" Foreground="{StaticResource TextSecondaryColor}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save" Style="{StaticResource ModernBtn}" Click="SaveBtn_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Cancel" Style="{StaticResource SecondaryBtn}" Click="CancelBtn_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>

