<Window x:Class="IcdControl.Client.StatsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ICD Statistics" Width="520" MinWidth="420" MinHeight="320"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource BackgroundColor}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="ICD Statistics" FontSize="22" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
            <TextBlock x:Name="IcdNameTxt" Margin="0,6,0,0" Foreground="{DynamicResource TextSecondaryColor}" TextWrapping="Wrap"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="0,15,0,0" Background="{DynamicResource SurfaceColor}" CornerRadius="10"
                BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Total Messages" Foreground="{DynamicResource TextSecondaryColor}"/>
                <TextBlock x:Name="TotalMessagesTxt" Grid.Column="1" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryColor}"/>

                <TextBlock Grid.Row="1" Text="Total Structs" Foreground="{DynamicResource TextSecondaryColor}"/>
                <TextBlock x:Name="TotalStructsTxt" Grid.Row="1" Grid.Column="1" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryColor}"/>

                <TextBlock Grid.Row="2" Text="Total Fields" Foreground="{DynamicResource TextSecondaryColor}"/>
                <TextBlock x:Name="TotalFieldsTxt" Grid.Row="2" Grid.Column="1" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryColor}"/>

                <TextBlock Grid.Row="3" Text="Estimated Size" Foreground="{DynamicResource TextSecondaryColor}"/>
                <TextBlock x:Name="EstimatedSizeTxt" Grid.Row="3" Grid.Column="1" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryColor}"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Close" Style="{StaticResource SecondaryBtn}" MinWidth="90" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
