<Window x:Class="IcdControl.Client.AdminPermissionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin - Permissions" Height="500" Width="800" WindowStartupLocation="CenterOwner" Background="{StaticResource BackgroundColor}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Manage ICD Permissions" FontSize="20" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
            <ComboBox x:Name="IcdCombo" Margin="0,15,0,0" Height="35" DisplayMemberPath="Name" SelectionChanged="IcdCombo_SelectionChanged"/>
        </StackPanel>

        <DataGrid x:Name="PermGrid" Grid.Row="1" Margin="0,15,0,0" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="User" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding StatusText}" Width="150"/>
                <DataGridTemplateColumn Header="Actions" Width="220">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" Click="GrantEdit_Click" Margin="0,0,5,0" Style="{StaticResource ModernBtn}" FontSize="11" Padding="8,4"/>
                                <Button Content="View" Click="GrantView_Click" Margin="0,0,5,0" Style="{StaticResource SecondaryBtn}" FontSize="11" Padding="8,4"/>
                                <Button Content="Revoke" Click="Revoke_Click" Foreground="Red" Style="{StaticResource SecondaryBtn}" FontSize="11" Padding="8,4"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" Content="Close" Click="Close_Click" HorizontalAlignment="Right" Margin="0,15,0,0" Style="{StaticResource SecondaryBtn}"/>
    </Grid>
</Window>