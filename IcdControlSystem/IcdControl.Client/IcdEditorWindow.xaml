<Window x:Class="IcdControl.Client.IcdEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:IcdControl.Models;assembly=IcdControl.Models"
        mc:Ignorable="d"
        Title="ICD Editor Studio" Height="850" Width="1200"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource BackgroundColor}">

    <Window.Resources>
        <ControlTemplate x:Key="DarkComboBoxTemplate" TargetType="ComboBox">
            <Grid>
                <ToggleButton Name="ToggleButton" 
                              BorderBrush="{TemplateBinding BorderBrush}" 
                              Background="{TemplateBinding Background}" 
                              Foreground="{TemplateBinding Foreground}"
                              Grid.Column="2" Focusable="false" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                                <Path x:Name="Arrow" Fill="{TemplateBinding Foreground}" HorizontalAlignment="Right" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z" Margin="0,0,10,0"/>
                            </Border>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
                <ContentPresenter Name="ContentSite" IsHitTestVisible="False"  Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="6,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left" />
                <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                    <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                        <Border x:Name="DropDownBorder" Background="{DynamicResource SurfaceColor}" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}"/>
                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                        </ScrollViewer>
                    </Grid>
                </Popup>
            </Grid>
        </ControlTemplate>

        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Template" Value="{StaticResource DarkComboBoxTemplate}"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <DataTemplate DataType="{x:Type models:DataField}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🔹" Margin="0,0,5,0" Foreground="#60A5FA"/>
                <TextBlock Text="{Binding Name}" Foreground="{DynamicResource TextPrimaryColor}"/>
                <TextBlock Text=" : " Foreground="{DynamicResource TextSecondaryColor}"/>
                <TextBlock Text="{Binding Type}" Foreground="{DynamicResource TextSecondaryColor}"/>
            </StackPanel>
        </DataTemplate>

        <HierarchicalDataTemplate DataType="{x:Type models:Message}" ItemsSource="{Binding Fields}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="✉️" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryColor}"/>
            </StackPanel>
        </HierarchicalDataTemplate>

        <HierarchicalDataTemplate DataType="{x:Type models:Struct}" ItemsSource="{Binding Fields}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📦" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryColor}"/>
            </StackPanel>
        </HierarchicalDataTemplate>

        <DataTemplate x:Key="FieldEditorTemplate">
            <StackPanel Margin="10">
                <Border BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1" Margin="0,0,0,15">
                    <TextBlock Text="Edit Field" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryColor}" Margin="0,0,0,10"/>
                </Border>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Name" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="Size (Bits)" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding SizeInBits, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Data Type" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,15,0,4"/>
                <ComboBox SelectedItem="{Binding Type}" 
                          ItemsSource="{Binding DataContext.AvailableTypes, RelativeSource={RelativeSource AncestorType=Window}}"
                          Style="{StaticResource DarkComboBox}"/>

                <Button Content="🗑 Remove Field" Click="RemoveItem_Click" Background="Transparent" BorderBrush="#EF4444" BorderThickness="1" Foreground="#EF4444" HorizontalAlignment="Left" Padding="10,5" Margin="0,30,0,0"/>
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="MessageEditorTemplate">
            <StackPanel Margin="10">
                <Border BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1" Margin="0,0,0,15">
                    <TextBlock Text="Edit Message" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryColor}" Margin="0,0,0,10"/>
                </Border>

                <TextBlock Text="Name" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

                <TextBlock Text="OpCode / ID" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

                <TextBlock Text="Description" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Height="80" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20"/>

                <StackPanel Orientation="Horizontal">
                    <Button Content="+ Add Field" Click="AddField_Click" Style="{StaticResource ModernBtn}" Margin="0,0,10,0"/>
                    <Button Content="🗑 Delete Message" Click="RemoveItem_Click" Background="Transparent" BorderBrush="#EF4444" BorderThickness="1" Foreground="#EF4444"/>
                </StackPanel>
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="StructEditorTemplate">
            <StackPanel Margin="10">
                <Border BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1" Margin="0,0,0,15">
                    <TextBlock Text="Edit Struct" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryColor}" Margin="0,0,0,10"/>
                </Border>

                <TextBlock Text="Struct Name" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

                <TextBlock Text="Description" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Height="80" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20"/>

                <StackPanel Orientation="Horizontal">
                    <Button Content="+ Add Field" Click="AddField_Click" Style="{StaticResource ModernBtn}" Margin="0,0,10,0"/>
                    <Button Content="🗑 Delete Struct" Click="RemoveItem_Click" Background="Transparent" BorderBrush="#EF4444" BorderThickness="1" Foreground="#EF4444"/>
                </StackPanel>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="ICD Editor" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryColor}" VerticalAlignment="Center" Margin="0,0,20,0"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="↩ Undo" Click="Undo_Click" Style="{StaticResource SecondaryBtn}" Margin="0,0,5,0"/>
                    <Button Content="↪ Redo" Click="Redo_Click" Style="{StaticResource SecondaryBtn}" Margin="0,0,15,0"/>
                    <Rectangle Width="1" Fill="{DynamicResource BorderColor}" Margin="0,4"/>
                    <Button Content="⬇ Export .h" Click="ExportHeader_Click" Style="{StaticResource SecondaryBtn}" Margin="15,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Last Modified by:" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,0,5,0"/>
                    <TextBlock x:Name="LastUserTxt" Text="Unknown" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryColor}" Margin="0,0,15,0"/>
                    <TextBlock Text="Contributors:" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,0,5,0"/>
                    <TextBlock x:Name="ContributorsCountTxt" Text="0" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryColor}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{DynamicResource BackgroundColor}" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <StackPanel Width="300" Margin="0,0,20,0">
                        <TextBlock Text="ICD Name" Foreground="{DynamicResource TextSecondaryColor}" FontSize="11" Margin="0,0,0,2"/>
                        <TextBox x:Name="NameTxt" FontSize="14" FontWeight="Medium"/>
                    </StackPanel>
                    <StackPanel Width="100">
                        <TextBlock Text="Version" Foreground="{DynamicResource TextSecondaryColor}" FontSize="11" Margin="0,0,0,2"/>
                        <TextBox x:Name="VersionTxt" FontSize="14"/>
                    </StackPanel>
                </StackPanel>

                <Expander Grid.Row="1" Grid.Column="0" Header="Description &amp; Notes" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,5,0,0">
                    <TextBox x:Name="DescTxt" Height="60" TextWrapping="Wrap" Margin="0,5,20,0"/>
                </Expander>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="20,0,20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{DynamicResource SurfaceColor}" CornerRadius="6" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                    <DockPanel>
                        <Border DockPanel.Dock="Top" Background="{DynamicResource SurfaceColor}" Padding="10" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1" CornerRadius="6,6,0,0">
                            <TextBlock Text="MESSAGES" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryColor}"/>
                        </Border>
                        <Button DockPanel.Dock="Bottom" Content="+ Add Message" Click="AddMessage_Click" Style="{StaticResource SecondaryBtn}" Margin="5"/>
                        <TreeView x:Name="MessagesTree" Background="Transparent" BorderThickness="0" SelectedItemChanged="MessagesTree_SelectedItemChanged" Padding="5"/>
                    </DockPanel>
                </Border>

                <Border Grid.Row="2" Background="{DynamicResource SurfaceColor}" CornerRadius="6" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                    <DockPanel>
                        <Border DockPanel.Dock="Top" Background="{DynamicResource SurfaceColor}" Padding="10" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1" CornerRadius="6,6,0,0">
                            <TextBlock Text="STRUCTS" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryColor}"/>
                        </Border>
                        <Button DockPanel.Dock="Bottom" Content="+ Add Struct" Click="AddStruct_Click" Style="{StaticResource SecondaryBtn}" Margin="5"/>
                        <TreeView x:Name="StructsTree" Background="Transparent" BorderThickness="0" SelectedItemChanged="StructsTree_SelectedItemChanged" Padding="5"/>
                    </DockPanel>
                </Border>
            </Grid>

            <Border Grid.Column="2" Background="{DynamicResource SurfaceColor}" CornerRadius="6" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ContentControl x:Name="DetailsPresenter" Margin="20"/>
                </ScrollViewer>
            </Border>
        </Grid>

        <Border Grid.Row="3" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,1,0,0" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock x:Name="StatusTxt" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryColor}" Margin="0,0,20,0"/>
                <Button Content="Save All Changes" Click="SaveBtn_Click" Style="{StaticResource ModernBtn}" Width="150" Margin="0,0,10,0"/>
                <Button Content="Close" Click="CancelBtn_Click" Style="{StaticResource SecondaryBtn}" Width="80"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>